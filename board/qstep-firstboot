#!/bin/sh
# First boot of a flashed QStep image (qstep-firstboot.service):
#  1. copy the LinuxCNC config from /opt/qstep/config into ~arduino (if absent),
#  2. back up the STM32 flash once (the stock Arduino loader on a fresh board),
#  3. flash the QStep firmware to the STM32.
# Marks /var/lib/qstep/firstboot-done only when everything succeeded, so a
# failed firmware flash is retried on the next boot.
set -eu
STATE=/var/lib/qstep
CFG=/home/arduino/qstep-config
FW=/opt/qstep/firmware/qstep-fw.elf
OCD="/opt/openocd/bin/openocd -s /opt/openocd -f openocd_gpiod.cfg"
mkdir -p $STATE

if [ ! -d $CFG ]; then
	cp -r /opt/qstep/config $CFG
	[ -f /home/arduino/.axisrc ] || cp /opt/qstep/config/axisrc /home/arduino/.axisrc
	chown -R arduino:arduino $CFG /home/arduino/.axisrc
	echo "qstep-firstboot: installed LinuxCNC config in $CFG"
fi

# BOOT0 low so resets boot the STM32 from flash (qstep-rt-tune does this too).
gpioset -c /dev/gpiochip1 -t0 37=0

if [ ! -f $STATE/mcu-flash-backup.bin ]; then
	(cd /opt/openocd && $OCD -c "init; reset halt; dump_image $STATE/mcu-flash-backup.bin.tmp 0x08000000 0x200000; reset run; shutdown")
	mv $STATE/mcu-flash-backup.bin.tmp $STATE/mcu-flash-backup.bin
	echo "qstep-firstboot: STM32 flash backed up to $STATE/mcu-flash-backup.bin"
fi

if (cd /opt/openocd && $OCD -c "program $FW verify reset exit" 2>&1) | grep -q "Verified OK"; then
	echo "qstep-firstboot: QStep firmware flashed"
	touch $STATE/firstboot-done
else
	echo "qstep-firstboot: firmware flash FAILED; will retry next boot" >&2
	exit 1
fi
